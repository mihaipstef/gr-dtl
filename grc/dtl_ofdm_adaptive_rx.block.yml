id: dtl_ofdm_adaptive_rx
label: OFDM Adaptive Receiver
flags: [ python ]
category: '[DTL]'

inputs:
-   domain: stream
    dtype: complex

outputs:
-   domain: stream
    dtype: byte
-   domain: stream
    dtype: complex
-   domain: message
    id: feedback

templates:
    imports: from gnuradio import dtl
    make: |-
        dtl.ofdm_adaptive_rx(rx_config)

file_format: 1
