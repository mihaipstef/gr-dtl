id: dtl_ofdm_adaptive_repack_bits_bb
label: Adaptive Repack
category: '[DTL]'

templates:
  imports: from gnuradio import dtl
  make: dtl.ofdm_adaptive_repack_bits_bb(${len_tag_key}, ${unpack})


parameters:
-   id: len_tag_key
    label: Length Tag Key
    dtype: string
-   id: unpack
    label: Unpack
    dtype: bool


inputs:
-   domain: stream
    dtype: byte

outputs:
-   domain: stream
    dtype: byte

file_format: 1
