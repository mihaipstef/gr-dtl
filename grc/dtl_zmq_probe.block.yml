file_format: 1
id: dtl_zmq_probe
label: zmq_probe
category: '[DTL]'

parameters:
-   id: address
    label: Address
    dtype: string
-   id: bind
    label: Bind
    dtype: bool
    default: 'True'

inputs:
-   domain: message
    id: in
    optional: true

templates:
    imports: from gnuradio import dtl
    make: dtl.zmq_probe(${address}, ${bind})
    
