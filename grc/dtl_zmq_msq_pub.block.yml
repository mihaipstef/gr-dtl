file_format: 1
id: dtl_zmq_msq_pub
label: zmq_msq_pub
category: '[dtl]'

parameters:
-   id: address
    label: Address
    dtype: string
-   id: bind
    label: Bind
    dtype: bool
    default: 'True'

inputs:
-   domain: message
    id: in
    optional: true

templates:
    imports: from gnuradio import dtl
    make: dtl.zmq_msq_pub(${address}, ${bind})
    
